---
description: wallet connection patterns
---
# Wallet Connection Patterns
- Use `@txnlab/use-wallet-react`.
- WalletProvider wrapped around App in main.tsx.
- Trigger connections via `handleConnect(walletId)`.
- Support Pera, Defly, and WalletConnect.
